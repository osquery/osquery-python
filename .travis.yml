language: python
sudo: false
cache:
  directories:
    - $HOME/.cache/pip
python:
  - 2.7
  - 3.6
  - 3.7
  - pypy
  - pypy3
install:
  - pip install -r requirements.txt
script:
  - python setup.py build
  - python setup.py test
